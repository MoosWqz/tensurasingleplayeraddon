# Recognition Entity Classification

This directory contains the exact Tensura 2.0.1.0 registry-ID inventory used by the package validator. The production gameplay data lives under `src/main/resources/data/moostensuraaddon/tags/entity_type/recognition/`.

Run from the repository root:

```text
python tools/datapack_validation/entity_classification/validate_entity_classification.py .
```
